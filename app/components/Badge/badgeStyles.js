import { StyleSheet } from "react-native";

const badge = {
  borderRadius: 100,
  backgroundColor: "#AD4545",
  width: 30,
  height: 30,
  justifyContent: "center",
  alignItems: "center"
};

const badgeText = {
  color: "#ffffff",
  fontSize: 15
};

export default StyleSheet.create({ badge, badgeText });
